apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: frequent-flyer
spec:
  template:
    metadata:
      name: frequent-flyer-v1
    spec:
      containers:
        - image: >-
            quay.io/mouachan/frequent-flyer:native-1.0
          livenessProbe:
            httpGet:
              path: /healthz
          readinessProbe:
            httpGet:
              path: /healthz
      imagePullSecrets:
        - name: quay-secret


